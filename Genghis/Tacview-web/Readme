Tacview files are processed and then uploaded to a web page so people can download them.

The included index.php file will read/find all .txt.acmi or .zip.acmi files in the directory it is in, and display them as a web page with an explanation and a list of files to click and download.

The location/username/pass/etc of the FTP server is set in stats.ini.

The tacview files are uploaded by the same periodic FTP upload routine as the radar files.  Radar files are sent to (FTP server main dir)/twc/radar. Tacview files go to (FTP server main dir)/twc/tacview.  That is the same directory where this index.html and .htaccess should go.

In addition to this, the TacviewRecorder.dll (installed in the Steam ...../core directory), Genghis-Class-TacviewRecorderImpl.cs, the ..../Fresh Input File/tacview directory and scripts within it (found in a separate directory in this repository), you'll need the files found in .../IL2CLOD dir/Tacview - especially the .csv file found there.  Those files are also in a separate directory in this repository.

Note that these files, and TacviewRecorder.dll are from from JDU/FlyBy's TacviewRecorder 2.0 - more info at

* https://www.youtube.com/watch?v=agcQxz1CHm0
* https://forum.il2sturmovik.com/topic/88789-tacview-recorder-20-add-on-is-available/
* https://drive.google.com/drive/folders/1t7_Ekt4fcp5lT7vz9W8QaY9O09JzbJwF
